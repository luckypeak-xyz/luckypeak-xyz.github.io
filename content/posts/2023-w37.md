---
author:
  name: ruifeng
  link: https://blog.luckypeak.xyz
  email: me@luckypeak.xyz
  avatar: /safari-pinned-tab.svg
title: Hello Ruifeng's Blog
subtitle: 念念不忘，必有回响。🫵 坚持下去！！！
date: 2023-09-16T22:44:41+08:00
lastmod: 2023-09-17
categories:
  - weekly
tags:
  - Presto
  - Go
  - 二分查找
draft: false
featuredImagePreview: /safari-pinned-tab.svg
featuredImage: /safari-pinned-tab.svg
---

## 摘

> We don't read, we scan.

-- [The 10 Most Expensive Design Mistakes (Ever)](https://adplist.substack.com/p/10-most-expensive-design-mistakes)

深有感触，最近要做一个飞书的报警消息，在其中加入 emoji 可以帮助更快的发现问题。当然，文本也少不了，主要用于搜索。

<!--more-->

## 题
[100033. 最大合金数](https://leetcode.cn/problems/maximum-number-of-alloys/description/)

- 直观的想法是从少到多枚举制造的合金数，看到多少的时候就造不出来了，但是时间复杂度不允许
- 枚举给了我们另外一个思路，即通过二分来枚举可以制造的最大合金数，因为造更多的合金一定要花更多的钱
- 既然要二分，那就要找到二分的上下界，下界显然是 0，上界可以直接取一个很大的数，更合理的可以是 min(stock) + budget，也就是假设所需金属只有 1 中，且花费也为 1 的情况
```python {title="最大合金数"}

class Solution:
    def maxNumberOfAlloys(self, n: int, k: int, budget: int, composition: List[List[int]], stock: List[int], cost: List[int]) -> int:
      ans = 0
      mx = min(stock) + budget       
      for comp in composition:
        def check(count: int) -> bool:
          totalCost = 0
          for i, st in enumerate(stock):
            lack = count * comp[i] - st
            if lack <= 0:
              continue
            totalCost += lack * cost[i]
            if totalCost > budget:
              return False
          return True
        left = 0
        right = mx + 1
        while left + 1 < right:
          mid = (left + right) // 2
          if check(mid):
            left = mid
          else:
            right = mid
        ans = max(ans, left)
      return ans
```

## 技
[Golang 中 defer 关键字的注意点](/posts/golang-defer-notice)

## 译

[【译】Presto - SQL on Everything](/posts/presto-sql-on-everything)